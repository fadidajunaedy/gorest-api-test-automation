describe("E2E: User Lifecycle Flow", () => {
  test("POST User should be success with valid data", async () => {
    // return user with id
  });

  test("GET User should be success with valid id", async () => {
    // getting with new user id above
  });

  test("PATCH User data update should be success", async () => {
    // update either name or status
    // get data and expect the change
  });

  test("DELETE User data should be success", async () => {
    // delete user by id
    // get data and expect that user not found
  });

  test("Verify User deletion should be success", async () => {
    // delete user by id
    // get data and expect that user not found
  });
});
